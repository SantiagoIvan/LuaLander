/// <summary>
/// Todos los items que puede guardar el InventoryManager, sin importar la categoria
/// (llaves, pociones, etc). Agregar nuevos valores aca a medida que se sumen nuevos
/// tipos de item -- no hace falta tocar InventoryManager para eso.
/// </summary>
public enum ItemType
{
    RedKey,
    BlueKey,
    GreenKey,
    YellowKey,
    Ammo
}
